def determine_winner(vignesh, charan):
    if vignesh == charan:
        return "NULL"
    elif (vignesh == 'R' and charan == 'S') or \
         (vignesh == 'P' and charan == 'R') or \
         (vignesh == 'S' and charan == 'P'):
        return "Vignesh"
    else:
        return "Charan"
vignesh, charan = input().split()
print(determine_winner(vignesh, charan))
